<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <title>トレーニング内容</title>
  <style>
    .container {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      padding: 2rem;
    }

    .post-details {
      flex: 2;
    }

    .comments-list {
      flex: 1;
      border: 1px solid #ccc;
      padding: 1rem;
    }

    .comment {
      border-bottom: 1px solid #ccc;
      padding-bottom: 1rem;
      margin-bottom: 1rem;
    }

    .comments-title {
      margin-bottom: 0.5rem;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="post-details">
      <h2>トレーニング内容</h2>
      <div>
        <h3><%= @post.title %></h3>
      </div>
      <div>
        <span><label>目標時間</label><%= @post.target_time %>分</span>
        <span><label>難易度</label><%= @post.level %></span>
        <span><label>トレーニングジャンル</label><%= @post.genre.name %></span>
      </div>
      <div>
        <label>内容</label>
        <%= @post.body %>
      </div>
      <div>
        <label>投稿日</label>
        <%= @post.created_at.strftime('%Y年%m月%d日') %>
      </div>
    </div>

    <div class="comments-list">
      <h4 class="comments-title">コメントリスト</h4>
      <div class="comment">
        <%= render "admin/comments/list", post: @post  %>
      </div>
    </div>
  </div>
  <div class="container">
    <%= link_to '投稿削除', admin_post_path(@post), method: :delete, data: { confirm: '本当に削除しますか？' }, class: 'btn btn-danger' %>
  </div>
</body>
</html>